{
  "name": "axia-3d",
  "private": true,
  "version": "0.1.0",
  "description": "AXiA 3D — blender보다 쉽고 sketchup보다 정확한 경량 3D 모델링 엔진",
  "license": "MIT",
  "workspaces": [
    "packages/axia-action-catalog",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "build:wasm": "npm run build:wasm --workspace web",
    "build:all": "npm run build:all --workspace web",
    "typecheck": "npm run typecheck --workspace web",
    "test": "npm run test --workspace web",
    "validate:ifc": "node scripts/ifc-external-validate.mjs"
  },
  "devDependencies": {
    "web-ifc": "0.0.77"
  }
}
